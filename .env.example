# Optional: enables the existing /api/auth/login endpoint.
# Hash with: node -e "console.log(require('node:crypto').createHash('sha256').update('YOUR_PASSWORD').digest('hex'))"
NMI_AUTH_EMAIL=admin@example.com
NMI_AUTH_PASSWORD_SHA256=replace_with_a_sha256_hash
NMI_AUTH_ROLE=viewer
